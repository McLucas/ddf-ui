<td>
    <div class="pull-left result-source" title="{{fieldName}}">{{fieldName}}</div>
    <div class="pull-right text-right">
        {{#each values}}
            <div class="field-value" data-field-value="{{value}}" data-value-count="{{count}}" data-field-name="{{../fieldName}}"><a href="#">{{value}} ({{count}})</a></div>
        {{/each}}


        <!--{{#is state "SUCCEEDED"}}-->
            <!--{{results}}<span class="hit-count">/{{hits}}</span> <span class="status-time">-->
            <!--{{#lt elapsed 60000}}-->
                <!--{{#lt elapsed 1000}}-->
                    <!--({{duration elapsed milliseconds=null}}ms)-->
                <!--{{else}}-->
                    <!--({{duration elapsed seconds=null}}s)-->
                <!--{{/lt}}-->
            <!--{{else}}-->
                <!--({{duration elapsed minutes=null}}m)-->
            <!--{{/lt}}-->
        <!--</span>-->
        <!--{{else}}-->
            <!--{{#is state "FAILED"}}-->
                <!--<span class="label label-important">failed</span>-->
            <!--{{else}}-->
                <!--{{#is canceled true}}-->
                    <!--<span class="label label-warning">canceled</span>-->
                <!--{{else}}-->
                    <!--<i class="fa fa-circle-o-notch fa-spin"></i>-->
                <!--{{/is}}-->
            <!--{{/is}}-->
        <!--{{/is}}-->
    </div>
</td>


<!--<div class="field-name"><b>{{fieldName}}</b></div>-->
<!--{{#each values}}-->
    <!--<div class="indent-12 field-value" data-field-value="{{value}}" data-value-count="{{count}}" data-field-name="{{../fieldName}}"><a href="#">{{value}} ({{count}})</a></div>-->
<!--{{/each}}-->
